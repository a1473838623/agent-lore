# agent-lore

> **让 AI 编码工具从人类的修正里自动学习仓库规范。**
> 你改了它写的代码，那个 diff 就是它该学的东西。

定位：**harness 扩展层**。不是 agent，不替代任何工具，靠标准扩展点挂在主流 harness 上。

命名与 [agent-beacon](https://github.com/a1473838623/agent-beacon) 成系列 —— 同一套方法论的第二个应用。

---

## 一、生态位

```
模型（Claude / GPT / …）
   ↑
harness（Claude Code / Codex / Cursor / Cline / openclaw / Hermes …）
   ↑  hook · MCP · 文件系统           ← 标准扩展点
扩展层（agent-beacon · agent-lore）    ← 本项目在这里
```

**为什么选这一层**：做模型没资源，做 harness 打不过大厂，而扩展层几乎是空的 ——
大家要么做模型、要么做 harness，很少有人专门做**跨 harness 的能力增强**。

**这一层唯一的硬约束是可移植**：一旦绑定某个 harness，就退化成那个 harness 的私有功能，
失去存在意义。所以下面的接入设计是三层降级，而不是单一实现。

---

## 二、要解决的问题

现有方案的规范全部**靠人手写**：

| 方案 | 规范从哪来 | 会不会腐烂 |
|---|---|---|
| CLAUDE.md / Cursor Rules / Copilot Instructions | 人手写 | **会**，写完就没人维护 |
| Hermes Agent 的 skill | agent 自主创建（**过程性记忆**，非仓库规范） | 不会，但**锁死在 Hermes 内** |
| **agent-lore** | **从人类修正的 diff 自动提炼** | **持续自更新** |

而每天都在产生一个**免费、真实、高质量的学习信号，却被完全丢弃**：

> Agent 写了代码，你改了三处。**那三处就是它没学会的东西。**
> 下一次，它还会那样写。

---

## 三、核心闭环

```
Agent 写文件 ──► 快照（内容 + 来源标记）
                      │
              人类随后修改同一文件          ← 免费、持续、外部信号
                      │
                    diff
                      │
             归因分类（LLM，三分类）
              ├─ feature 业务变更  → 丢弃（噪声，占大多数）
              ├─ bug     修 AI 的错 → pitfall 候选
              └─ style   规范修正   → convention 候选
                      │
          同类重复 ≥N 次 → 提炼规范 → 人工确认 → 入库
                      │
        下次编辑前，按文件路径/符号带外注入
                      │
              度量：同类修正是否复发        ← 闭环验证
```

**②和⑤缺一不可**：只采集不注入是日记；只注入不度量是玄学。

---

## 四、四个真难题（面试主战场）

### ① 归因：怎么区分「AI 写错了」和「需求变了」

**噪声占绝大多数** —— 人对文件的改动大部分是业务演进，不是在纠正 AI。

- 三分类 prompt + **强制输出置信度**，低于阈值直接丢弃
- 只在 agent 写入后的**时间窗口内**（如 30 分钟）采集，超窗的改动视为独立开发
- **宁可漏，不可错**：学到一条错规范的代价 >> 漏掉一条对规范

> **归因准确率是这个项目的天花板。** 这一条答不好，整个项目就是空中楼阁。

### ② 泛化：几次修正才算规范

一次修正是个例，不能立刻升格。

- 同类修正累计 **≥3 次**才提升为 convention 候选
- 「同类」的判定：LLM 把 diff 归纳成一句规范描述，再在**描述层面**做合并去重
  （不是在 diff 文本层面 —— 同一条规范的 diff 可以长得完全不同）
- 阈值定高学不到，定低学到噪声 —— **阈值本身应可配，并用第④条的指标来调**

### ③ 冲突：新规范和旧规范矛盾时

- 检测到矛盾 → **不自动覆盖**，标记冲突交人确认
- 默认策略：**新的优先**（规范会演进，旧的往往是过时的那个）
- 保留历史，规范带时间戳与来源 diff，可回溯「为什么有这条」

### ④ 验证：怎么证明真的有用

核心指标：**修正复发率**

> 某条规范入库并开始注入后，**同类人类修正的发生次数应当下降。**
> 不下降 = 这条规范没用，或者注入没生效 —— 两种情况都要能区分出来。

配套记录：注入条目、注入 token 数、注入后该文件是否仍发生同类修正。

**这就是知识库的评测。** 「我做了个知识库」和「我测了我的知识库，并知道阈值该设多少」是两个层次。

---

## 五、跨 harness 接入：三层降级

不同 harness 的扩展能力差别很大，所以按能力降级，**保底方案不依赖任何 harness 特性**：

| 层 | 机制 | 适用 | 能力 |
|---|---|---|---|
| **L1 最完整** | `PreToolUse` / `PostToolUse` hook | Claude Code | 精确知道哪些写入来自 agent；**编辑前带外注入** |
| **L2 中等** | MCP tools（`lore_check` / `lore_write`） | Codex · Cursor · Cline · Windsurf · openclaw 等 MCP 客户端 | 靠 agent 主动调用，注入时机不可控 |
| **L3 保底** | **git working-tree 监听** | **任何工具，包括没有扩展点的** | 纯文件系统 + git，零 harness 依赖 |

**L3 是可移植性的关键**：只要 agent 改了文件，工作区就有变化。
未经 L1/L2 上报的变更即视为人类修正 —— 这个推断在任何 harness 下都成立。

> agent-beacon 已经验证过这个路子（`beacon watch` 覆盖非 agent 活动），机制可直接复用。

### 与 Hermes Agent 的结构性差异

Hermes 是 **harness**，学习闭环内建于其运行时 —— **想要它就得换掉现有工具**。
agent-lore 在扩展层，靠 hook / MCP / 文件系统三条标准通道，**跨 harness 可移植，甚至能装在 Hermes 上**。

这不是实现选择，是结构差异：harness 厂商没有动机把自己的学习能力做成跨 harness 的。

---

## 六、知识分层

```
~/.agent-lore/
├── convention/<repo>.md    仓库级规范   → 全量常驻注入（漏检即失效）
├── pitfall/<repo>/*.md     踩坑记录     → 按文件/符号带外注入
├── candidate/*.jsonl       未达阈值的候选
└── metrics.jsonl           注入与复发记录
```

存储用 **markdown + JSONL**，索引只是加速层：可读、可 git、可手改、可 Grep。
**索引损坏删掉重建即可 —— 数据永不锁在索引里。**

检索**刻意不上向量**：检索词天然是专有名词（类名、方法名、配置项），关键词精确率高于语义检索。
规模到千级再换，接口不变。

> 旁证：Hermes 的跨会话召回用的也是 **FTS5 全文检索**，不是向量库。

---

## 七、设计约束（与 agent-beacon 共享）

1. **无关时零 token** —— 未命中就完全不注入，而不是注入「无相关知识」
2. **注入有硬预算** —— 单次 ≤300 token，超出按相关性截断
3. **fail-open** —— 查询超时、失败、格式错误一律放行，绝不阻塞编辑
4. **人工确认闸** —— 规范入库前必须人确认；宁可漏，不可错
5. **不改 agent 本身** —— 只在工具调用边界介入

---

## 八、v0 范围

| 做 | 不做 |
|---|---|
| L1 hook 采集 + L3 文件监听保底 | L2 MCP（v1） |
| LLM 三分类归因 + 置信度阈值 | 自动聚类（v0 用 LLM 描述合并） |
| 计数阈值（≥3）+ 人工确认入库 | 冲突自动消解 |
| 按路径/符号注入 + 300 token 预算 | 可视化看板（v1） |
| `metrics.jsonl` 记录复发率原始数据 | 指标看板 |

**约 300–500 行。** 所有机制都是现成的，难的全在判断 —— 这正是好项目的形状。

---

## 九、简历条目草稿

> **agent-lore · AI 编码规范自学习扩展（个人项目 · 开源）**
> 背景：现有 AI 编码工具的规范全靠人手写且会腐烂；而「人类修正 AI 输出」这一免费且高质量的学习信号被完全丢弃。
> 本项目定位于 **harness 扩展层**，不替代任何工具。
>
> 1. **从人类修正中学习**：以 hook 快照 agent 写入，捕获随后的人工修改 diff 作为学习信号，
>    经 LLM 三分类归因（规范修正 / 修复缺陷 / 业务变更），仅前两类入库
> 2. **归因与泛化控制**：置信度阈值 + 采集时间窗 + 同类修正累计 ≥3 次方可升格为规范，
>    并以「宁可漏、不可错」为原则设定阈值——学到一条错规范的代价远高于漏掉一条
> 3. **带外注入**：`PreToolUse` hook 在编辑发生前按文件路径与符号召回并注入，agent 无感知；
>    单次 ≤300 token 预算、未命中零注入、查询失败一律放行
> 4. **跨 harness 可移植**：hook / MCP / git 工作区监听三层降级接入，
>    保底方案不依赖任何 harness 特性，适配 Claude Code、Codex、Cursor 等
> 5. **闭环可度量**：以「修正复发率」验证规范有效性——规范注入后同类人工修正应下降，
>    并据此反调泛化阈值

⚠️ **写「开源」必须真有仓库。** 没做出来就写「设计」，不写「开发」。

---

## 十、预判追问

| 追问 | 答 |
|---|---|
| **这不就是 Hermes 吗？** | Hermes 是 harness，学习闭环绑定其运行时，要用就得换工具；本项目在扩展层，三层降级接入，跨 harness 可移植。学习单元也不同：Hermes 学过程性 skill，本项目学仓库级规范 |
| 和 Cursor Rules / CLAUDE.md 区别？ | 那些**靠人手写、写完即腐烂**；本项目从修正 diff 自动提炼并持续更新，且构成可度量闭环 |
| 怎么区分「AI 写错」和「需求变了」？ | 三分类归因 + 置信度阈值 + 采集时间窗；**噪声占多数，所以宁可漏不可错**。归因准确率是项目天花板 |
| 学到错规范怎么办？ | 三道闸：置信度阈值、≥3 次累计、人工确认。且规范保留来源 diff 可回溯撤销 |
| 怎么证明有用？ | 修正复发率——规范注入后同类修正应下降。不下降就要区分是规范没用还是注入没生效 |
| 为什么不让模型自己检索？ | **Agent 不知道自己不知道**，不会主动查「这个文件以前踩过什么坑」。依赖主动检索 = 依赖它想起来 |
| 为什么不上向量库？ | 检索词是专有名词，关键词精确率更高；规范类根本不该检索——漏一次即失效。Hermes 用的也是 FTS5 |
| 为什么不微调模型？ | 规范变化频繁、样本量小、且需要可解释（人要能看到学到了什么并否决）。检索注入在这三点上都优于微调 |
